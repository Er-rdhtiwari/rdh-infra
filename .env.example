# Core
AWS_REGION=ap-south-1
NAME_PREFIX=platform
ENVIRONMENT=dev
ROOT_DOMAIN=rdhcloudlab.com
CREATE_SUBDOMAIN_ZONE=true

# Terraform state (bootstrap output can update these)
TF_STATE_BUCKET=platform-tf-state-demo
TF_STATE_DYNAMO_TABLE=platform-tf-lock
TF_STATE_KEY_BOOTSTRAP=bootstrap/terraform.tfstate
TF_STATE_KEY_PLATFORM=platform/terraform.tfstate

# Networking
VPC_CIDR=10.20.0.0/16
PUBLIC_SUBNET_CIDRS=10.20.1.0/24,10.20.2.0/24,10.20.3.0/24
PRIVATE_SUBNET_CIDRS=10.20.11.0/24,10.20.12.0/24,10.20.13.0/24

# EKS
K8S_VERSION=1.28
NODE_INSTANCE_TYPES=t3.medium
NODE_MIN_SIZE=1
NODE_MAX_SIZE=3
NODE_DESIRED_SIZE=1

# Outputs to feed helm values (fill after platform apply)
VPC_ID=
ALB_CONTROLLER_IAM_ROLE_ARN=
EXTERNALDNS_IAM_ROLE_ARN=
EBS_CSI_IAM_ROLE_ARN=
ACM_CERT_ARN=

# Jenkins
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=ChangeMe123!
JENKINS_STORAGE_SIZE=20Gi

# ExternalDNS
EXTERNALDNS_TXT_OWNER_ID=platform-dev

# PoC defaults for script convenience
POC_HELM_REPO=
POC_HELM_REPO_NAME=
POC_HELM_CHART=
POC_HELM_VERSION=
POC_NAMESPACE_PREFIX=poc
